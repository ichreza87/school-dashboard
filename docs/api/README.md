# API Contract

OpenAPI 3.0 di `openapi.yaml`. Generate client/server via `npx openapi-typescript` atau Swagger UI (`/api/docs` future).

Validasi: semua POST pakai Zod (lihat `backend/src/index.ts` studentSchema). List endpoint lengkap di `docs/api.md`.
