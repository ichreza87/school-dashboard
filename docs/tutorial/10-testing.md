# 10 - Testing

Unit: ``node tests/sync-engine.test.js`` (diff engine), ``node tests/grade.test.js`` (formula), ``node tests/attendance.test.js`` (alert), ``node tests/report-template.test.js``.

Integration: seed + API via supertest (future). Jalankan ``npm test`` di root. Semua test harus print Passed/Failed.
