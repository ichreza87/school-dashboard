# Synchronization

Diff engine membandingkan lokal vs remote per key (nisn/nuptk). Status: NEW/UPDATED/UNCHANGED/CONFLICT/ERROR. Konflik resolved per-field, tanpa overwrite massal. History di /api/sync/history.
